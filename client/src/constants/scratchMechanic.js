/** Must match server `SCRATCH_SYMBOL_COUNT` (scratchTierMath.js). */
export const SCRATCH_SYMBOL_COUNT = 16;

/** Must match server `JACKPOT_MAX_MATCH_COUNT` (scratchTierMath.js). */
export const JACKPOT_MAX_MATCH_COUNT = 9;
